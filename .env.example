# Example environment variables for local development and deployment
# MODEL_PROVIDER can be 'local' or 'openai'
MODEL_PROVIDER=local

# If you plan to use OpenAI, set MODEL_PROVIDER=openai and provide the key below
OPENAI_API_KEY=

# Allowed origins for CORS in production (comma separated)
ALLOWED_ORIGINS=http://localhost:3000
# Example environment variables for local development
MODEL_PROVIDER=local
# If you want to use OpenAI: set MODEL_PROVIDER=openai and provide the key below
OPENAI_API_KEY=
# Backend
OPENAI_API_KEY=
MODEL_PROVIDER=local  # or 'openai'
